<%= form_for :employee, url: employees_path do |f| %>
  <% if @employee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@employee.errors.count, "error") %> prohibited this postfrom being saved:</h2>
      <ul>
        <% @employee.errors.full_messages.each do |mesg| %>
          <li><%= mesg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<% end %>

<%= form_for @employee do |f| %>
  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  
  <p>
    <%= f.submit %>
  </p>

<% end %>
